---
const { logo, title, name, link } = Astro.props;
---

<a
  class="flex justify-left md:justify-center items-center gap-4 font-bold text-white transition ease-in-out hover:scale-110"
  href={link}
>
  <img class="w-8 h-8" src={logo} alt={title} />
  <p class="text-xl md:text-2xl">{name}</p>
</a>
